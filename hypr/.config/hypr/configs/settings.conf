# --- SETTINGS ---
input {
    kb_layout = us
    follow_mouse = 1
    touchpad {
        natural_scroll = false
    }
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

dwindle {
    pseudotile = true
    preserve_split = true
    smart_split = true
    force_split = 2
    use_active_for_splits = true
}

master {
    new_status = master
    new_on_top = false
    mfact = 0.55
}

# New Gesture System (v0.54.2+)
# Format: gesture = <fingers>, <direction>, <dispatcher> [args...]
# Example: gesture = 3, horizontal, workspace
# To disable swiping, simply do not define a gesture.

misc {
    force_default_wallpaper = 0 # Set to 0 to disable the anime mascot wallpapers
    disable_hyprland_logo = true
    disable_splash_rendering = true
    vfr = true
}

xwayland {
    force_zero_scaling = true
    use_nearest_neighbor = true
}

# cursor {
#     no_hardware_cursors = true
#     sync_gaps_workspaces = true
# }
